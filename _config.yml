title: "High-Accuracy Computing on Low-Precision Hardware (HCLH)"
description: "SC Workshop: precision-aware scientific computing on modern low-precision hardware"
theme: minima

# Main navigation is defined in _data/nav.yml (single-page anchors + external Venue).

# Optional: show downloads (e.g., proposal PDF) on the homepage via custom links
minima:
  social_links:
    - { platform: github, user_url: "https://github.com/YOUR-ORG/YOUR-REPO" }
